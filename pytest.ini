[pytest]
testpaths = tests
markers =
    unit: Unit tests
    observability: Observability validation tests
    bdd: Behavior-driven development tests
    integration: Integration tests requiring a real running instance (no HTTP mocking)
